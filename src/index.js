// Orchestrates the discover -> trace -> report pipeline.
// TODO: implement. See src/discover, src/trace, src/report.
export async function scan(targetPath, options = {}) {
  console.log(`cvetrace: scan not yet implemented (target: ${targetPath})`);
}
