# node-fixture-project

TODO: minimal `package.json` + `package-lock.json` pinning a package/version with a
known, stable CVE (verify against OSV.dev at implementation time), used by `test/e2e.test.js`.
