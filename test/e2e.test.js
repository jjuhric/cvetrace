import test from "node:test";

// TODO: run the built CLI against test/fixtures/{node,java,python}-fixture-project
// and assert the known-CVE package/version pinned in each fixture is reported.
test.todo("cvetrace scan reports known CVEs in each fixture project");
